/**
 * Single source of truth for the flat delivery fee (DZD).
 * Consumed by Cart, Checkout, and OrderConfirmation so the displayed
 * fee can never drift between surfaces.
 */
export const SHIPPING_COST_DZD = 500;
